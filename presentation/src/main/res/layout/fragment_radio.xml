<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.content.radio.RadioFragment">

    <ImageView
        android:id="@+id/eztandaImv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toTopOf="@+id/playStopBtn"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/img_eztanda"
        tools:ignore="ContentDescription" />

    <ImageView
        android:id="@+id/playStopBtn"
        android:layout_width="70dp"
        android:layout_height="70dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/eztandaImv"
        app:srcCompat="@drawable/ic_play"
        tools:ignore="ContentDescription" />

    <ProgressBar
        android:id="@+id/progressBar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="@+id/playStopBtn"
        app:layout_constraintEnd_toEndOf="@+id/playStopBtn"
        app:layout_constraintStart_toStartOf="@+id/playStopBtn"
        app:layout_constraintTop_toTopOf="@+id/playStopBtn"
        tools:visibility="visible" />

</androidx.constraintlayout.widget.ConstraintLayout>